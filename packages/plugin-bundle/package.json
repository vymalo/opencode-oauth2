{
  "name": "@lightbridge/plugin-bundle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "clean": "rm -rf dist",
    "lint": "biome lint .",
    "typecheck": "echo 'TODO: typecheck plugin-bundle'",
    "test": "echo 'TODO: test plugin-bundle'",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "devDependencies": {
    "@lightbridge/opencode-plugin": "workspace:*",
    "builtin-modules": "^5.0.0",
    "rolldown": "^1.0.2"
  }
}
