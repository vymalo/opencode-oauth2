{
  "name": "opencode-oauth2-workspace",
  "version": "0.8.1",
  "private": true,
  "description": "OpenCode Toolbelt — the @vymalo plugin suite for OpenCode: OAuth2/OIDC auth, model-metadata enrichment, rate-limit awareness, and browser automation.",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "biome lint packages apps test-env *.json",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "coverage": "pnpm -r --if-present coverage",
    "test:env:up": "docker compose -f test-env/docker-compose.yml up -d --wait",
    "test:env:down": "docker compose -f test-env/docker-compose.yml down -v",
    "test:integration:run": "INTEGRATION_MODELS_INFO_URL=${INTEGRATION_MODELS_INFO_URL:-http://127.0.0.1:18080/v1/models} pnpm -r --if-present test:integration",
    "test:integration": "pnpm test:env:up && (pnpm test:integration:run; status=$?; pnpm test:env:down; exit $status)",
    "format": "biome format --write packages apps test-env *.json",
    "format:check": "biome format packages apps test-env *.json"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/node": "^25.9.1",
    "@vitest/coverage-v8": "4.1.7",
    "typescript": "^6.0.3"
  }
}
