{
  "name": "lightbridge-opencode",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace for the OpenCode OAuth2 model sync plugin.",
  "packageManager": "pnpm@10.26.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "biome lint .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
